{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome", // or "edge" for Edge
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://localhost:5500",
            "webRoot": "${workspaceFolder}"
        }
    ]
}